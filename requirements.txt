# Framework web
Django>=5.0,<6.0

# API REST
djangorestframework>=3.15,<4.0
djangorestframework-simplejwt>=5.3,<6.0

# CORS (futur frontend)
django-cors-headers>=4.4,<5.0

# Politique de sécurité du contenu (en-têtes CSP)
django-csp>=3.8,<4.0

# Documentation OpenAPI et interface Swagger
drf-spectacular>=0.27,<1.0

# Lecture des variables d'environnement (.env)
django-environ>=0.11,<1.0

# Base de donnees PostgreSQL
psycopg2-binary>=2.9,<3.0

# Traitement d'images (avatars, images de posts)
Pillow>=10.0,<12.0

# Serveur WSGI de production
gunicorn>=22,<24

# Service des fichiers statiques en production (collectstatic + cache HTTP)
whitenoise>=6.7,<7.0

# Géolocalisation des adresses IP (base GeoLite2 MaxMind, facultative)
geoip2>=4.6,<5.0

# Mesure de couverture des tests (dev et CI)
coverage>=7,<8

# Outils qualité (lint et audit de dépendances, dev et CI)
ruff>=0.6,<1.0
pip-audit>=2.7,<3.0
