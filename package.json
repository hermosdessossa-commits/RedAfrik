{
  "name": "redafrik-e2e",
  "private": true,
  "description": "Tests de bout en bout de l'interface RedAfrik (Playwright)",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}